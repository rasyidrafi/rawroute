# RawRoute runtime and dashboard
STORAGE_BACKEND=postgres
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=replace-with-a-strong-password
DEFAULT_PROXY_API_KEY=sk-replace-with-a-gateway-key
SESSION_SECRET=replace-with-at-least-32-random-characters
CREDENTIAL_ENCRYPTION_KEY=replace-with-a-separate-encryption-key
TIMEZONE=UTC
NEXT_PUBLIC_TIMEZONE=UTC

# Local durable storage. PostgreSQL stores the full RawRoute document tree;
# Redis stores Codex quota cache and lock entries.
POSTGRES_DB=rawroute
POSTGRES_USER=rawroute
POSTGRES_PASSWORD=replace-with-a-long-random-database-password
DATABASE_URL=postgresql://rawroute:replace-with-a-long-random-database-password@postgres:5432/rawroute
DATABASE_COLLECTION_PREFIX=rawroute
DATABASE_POOL_MAX=20
REDIS_URL=redis://redis:6379
DATABASE_CHILD_READ_CONCURRENCY=16
DATABASE_ANALYTICS_READ_CONCURRENCY=8
RAWROUTE_IMAGE=docker.io/rasyidrafi/rawroute:latest
RAWROUTE_HOST_PORT=8080
# RawRoute retrieval caches only. CLIProxyAPI owns provider rate limits, retries, and routing.
WORKSPACE_CACHE_TTL_MS=60000
WORKSPACE_NEGATIVE_CACHE_TTL_MS=10000
MAX_WORKSPACE_CACHE_ENTRIES=256
BUDGET_CACHE_TTL_MS=60000
BUDGET_COUNTER_CACHE_TTL_MS=30000
DASHBOARD_CACHE_TTL_MS=30000
PRICING_CATALOG_CACHE_TTL_MS=60000
PRICING_ADMIN_CACHE_TTL_MS=5000
PRICING_JOBS_CACHE_TTL_MS=2000
CODEX_USAGE_LOCAL_CACHE_TTL_MS=300000
USAGE_ROLLUP_GRANULARITIES=hourly,daily

# RawRoute budget admission and usage estimation
BUDGET_DEFAULT_OUTPUT_TOKENS=4096
BUDGET_INPUT_BYTES_PER_TOKEN=3
# Used only when a successful provider response has no usage metadata and no
# exact in-process history is available. The primary fallback is a lower
# empirical quantile from pooled exact key/model events.
BUDGET_ESTIMATED_CACHE_READ_RATIO=0.5
BUDGET_MISSING_USAGE_MAX_COST_MICROS=2000000
BUDGET_MISSING_USAGE_QUANTILE=0.25
BUDGET_PREDICTION_PRIOR_STRENGTH=500
BUDGET_PREDICTION_BASELINE_MIN_SAMPLES=30
BUDGET_PREDICTION_SAMPLE_LIMIT=2048
# Direct OpenAI/Codex missing-usage calibration. It uses nearby exact request
# payload sizes and observed token/cache ratios; cache-write is never invented
# when the exact history has no cache-write tokens.
BUDGET_PAYLOAD_PREDICTION_ENABLED=1
BUDGET_PAYLOAD_PREDICTION_MIN_SAMPLES=3
BUDGET_PAYLOAD_PREDICTION_MAX_NEIGHBORS=31

# Private CLIProxyAPI service; none of these values are sent to browsers.
CLIPROXY_URL=http://cli-proxy-api:8317
CLIPROXY_MANAGEMENT_KEY=replace-with-a-long-internal-management-key
CLIPROXY_API_KEY=replace-with-the-key-configured-in-cliproxy
RAWROUTE_PUBLIC_URL=http://localhost:8080
CLI_PROXY_IMAGE=eceasy/cli-proxy-api:latest

# Compose-mounted CLIProxyAPI state. The image itself is pulled from the registry.
CLI_PROXY_CONFIG_PATH=./cliproxy/config.yaml
CLI_PROXY_AUTH_PATH=./cliproxy/auths
CLI_PROXY_LOG_PATH=./cliproxy/logs
CLI_PROXY_PLUGIN_PATH=./cliproxy/plugins
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=replace-with-a-stable-base64-encoded-32-byte-key
DEPLOYMENT_VERSION=replace-with-the-build-identifier
